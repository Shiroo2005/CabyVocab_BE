export enum NotificationType {
  CHANGE_PASSWORD = 'Change password',
  VOTE = 'Vote',
  COMMENT = 'Comment',
  ORDER = 'Order'
}

export enum NotificationTarget {
  ALL = 'All',
  ONLY_USER = 'Only User',
  SEGMENT = 'Segment'
}
