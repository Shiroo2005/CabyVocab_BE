export enum NotificationType {
  CHANGE_PASSWORD = 'Change password',
  VOTE = 'Vote',
  COMMENT = 'Comment'
}

export enum NotificationTarget {
  ALL = 'All',
  ONLY_USER = 'Only User',
  SEGMENT = 'Segment'
}
